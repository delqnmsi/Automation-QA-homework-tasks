
# Environment / target application
environment=qa
base.url=https://www.amazon.co.uk/

# Browser
browser=chrome
headless=false

# Timeouts (seconds)
implicit.wait.seconds=0
explicit.wait.seconds=10
element.load.seconds=5
page.load.timeout.seconds=30
